self.addEventListener('push',e=>{let d={};try{d=e.data?.json()||{}}catch{};e.waitUntil(self.registration.showNotification(d.title||'Shop Importadora',{body:d.body||'Nova atualização disponível.',icon:'/icon-192.png',badge:'/icon-192.png',tag:d.tag||'shop',data:{url:d.url||'/?open=notes'}}))});self.addEventListener('notificationclick',e=>{e.notification.close();let u=e.notification.data?.url||'/?open=notes';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(cs=>{let c=cs.find(x=>x.url.includes(self.location.origin));if(c){c.focus();c.navigate(u);return}return clients.openWindow(u)}))});